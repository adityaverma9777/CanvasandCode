{
  "name": "canvas2code-monorepo",
  "version": "1.0.0",
  "description": "Canvas2Code Full Stack Application",
  "scripts": {
    "setup": "npm install concurrently && cd client && npm install && cd ../server && npm install",
    "dev": "npx concurrently \"cd server && npm run dev\" \"cd client && npm run dev\" --names \"SERVER,CLIENT\" --prefix-colors \"yellow,cyan\""
  },
  "author": "We Crack It",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}